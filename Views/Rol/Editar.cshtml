@model Inmobiliaria10.Models.Rol

@{
    ViewData["Title"] = "Editar Rol";
}

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-6"> 
            <div class="card form-card">
                <div class="card-header form-card-header">
                    <h4 class="form-title text-center">@ViewData["Title"]</h4>
                </div>
                <div class="card-body">
                    <form asp-action="Editar" method="post">
                        <input type="hidden" asp-for="IdRol" />

                        <div class="mb-3">
                            <label asp-for="DenominacionRol" class="form-label w-100 text-center"></label>
                            <input asp-for="DenominacionRol" class="form-control text-center"
                                   oninput="this.value = this.value.toUpperCase()" autocomplete="off"/>
                            <span asp-validation-for="DenominacionRol" class="text-danger"></span>
                        </div>

                        <div class="d-flex justify-content-between mt-4">
                            <a asp-action="Index" class="btn btn-back">
                                <i class="fa-solid fa-arrow-left me-2"></i> Cancelar
                            </a>
                            <button type="submit" class="btn btn-save">
                                <i class="fa-solid fa-floppy-disk me-2"></i> Guardar Cambios
                            </button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
