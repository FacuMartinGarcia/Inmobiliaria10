@model Inmobiliaria10.Models.Inquilino

@{
    ViewData["Title"] = "Eliminar Inquilino";
}

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <div class="card form-card border-danger">
                <div class="card-header form-card-header bg-danger text-white">
                    <h4 class="form-title text-white">@ViewData["Title"]</h4>
                </div>
                <div class="card-body text-center">
                    <h4 class="mb-4">¿Seguro que querés eliminar este inquilino?</h4>

                    <p class="mb-4">
                        <strong>@Model.ApellidoNombres</strong>  
                        <br />
                        <span class="text-muted">(@Model.Documento)</span>
                    </p>

                    <form asp-action="Borrar" method="post" class="d-flex justify-content-center gap-3">
                        <input type="hidden" asp-for="IdInquilino" />

                        <button type="submit" class="btn btn-danger px-4">
                            <i class="fa-solid fa-trash me-2"></i> Eliminar
                        </button>

                        <a asp-action="Index" class="btn btn-back px-4">
                            <i class="fa-solid fa-arrow-left me-2"></i> Cancelar
                        </a>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>
