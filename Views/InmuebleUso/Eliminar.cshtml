@model Inmobiliaria10.Models.InmuebleUso

@{
    ViewData["Title"] = "Eliminar Uso de Inmueble";
}

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-6"> 
            <div class="card form-card border-danger">
                <div class="card-header form-card-header bg-danger text-white">
                    <h4 class="form-title">@ViewData["Title"]</h4>
                </div>
                <div class="card-body text-center">
                    <p class="mb-4">¿Está seguro que desea eliminar el siguiente uso de inmueble?</p>

                    <h5 class="fw-bold">@Model.DenominacionUso</h5>

                    <!-- 🔹 Usa .form-delete y mensaje personalizado -->
                    <form asp-action="Eliminar" method="post" class="form-delete"
                          data-msg="¿Confirmás eliminar el uso de inmueble '@Model.DenominacionUso'?">
                        @Html.AntiForgeryToken()
                        <input type="hidden" asp-for="IdUso" />

                        <div class="d-flex justify-content-between mt-4">
                            <a asp-action="Index" class="btn btn-back">
                                <i class="fa-solid fa-arrow-left me-2"></i> Cancelar
                            </a>
                            <button type="submit" class="btn btn-danger">
                                <i class="fa-solid fa-trash me-2"></i> Eliminar
                            </button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>
